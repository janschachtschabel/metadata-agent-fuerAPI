/**
 * Global debug flag for the metadata-agent web component.
 * Toggled via <metadata-agent-canvas debug="true">.
 * When disabled (default), no debug output is written to the console.
 */
export const WidgetDebug = { enabled: false };
